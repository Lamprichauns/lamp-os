# Development environment variables
# These are intentionally left in the repo

# HTTP server URL for development
VITE_SERVER_HTTP=http://localhost:3001

# WebSocket server URL for development  
VITE_SERVER_WS=ws://localhost:3001
