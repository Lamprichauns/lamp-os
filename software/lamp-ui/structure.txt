{
  "lamp": {
    "name": "snafu", //lower case restricted to 12 characters - required. regex [a-z]+
    "brightness": 80, // number between 0, 100. required
    "disposition": 1, // will send a enum names of numbers to names. optional. defaults to aloof. validate submitted numbers exist in list.
    "homeMode": false // boolean validation. required. default false
    "password": "foobar1234" //optional string restricted from 8-10 chars [a-z][A-Z][0-9]+
  },
  "shade": {
    "px": 18, // number from 1 -> 50 - required
    "colors": ["#673a8311"] // array size of 1 for now 32 bit value encoded as hex (RGBW). at least one option required 
    "ac": 1, // the active shade color for sharing, 0 by default, won't be specified for a while
  },
  "base": {
    "px": 24, // number from 1 -> 50 - required
    "colors": ["#7c4400ff", "#9B2E00C5"], //up to 5 32 bit value encoded as hex (RGBW). at least one option required
    "ac": 1, // the active base color for sharing, 0 by default
    "knockout": [ // only differences from 100% - pixels must be bewtween n...pixelcount. default empty array
      { "p": 4, "b": 50 },
      { "p": 5, "b": 75 }
    ]
  },
  "reactions": [ // will send supported enum names in a service. default empty array, validate submitted numbers exist in list
    { "id": "century", "r" : 1 },
    { "id": "vamp", "r": 2 },
    { "id": "draper", "r": 3 }
  ],
  "stage": {
    "enabled": false //boolean validation. default true. required.
  }
}